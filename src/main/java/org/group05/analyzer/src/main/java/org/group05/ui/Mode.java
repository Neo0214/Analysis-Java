package org.group05.ui;

public enum Mode {
    //COMMAND : to enter the project root path or quit
    //EXEC : after getting and setting the project root path, get the parameter for analyzer
    //QUIT : quit the program, stop everything.
    EXEC, COMMAND, QUIT
}
